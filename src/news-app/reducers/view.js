import ActionTypes from './../actions/Types';

const view = (state = {}, action) => {
    const { type, data } = action;

    switch (type) {
        default:
            return state;
    }
};

export default view;
