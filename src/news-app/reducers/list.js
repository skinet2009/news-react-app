import ActionTypes from './../actions/Types';

const list = (state = {}, action) => {
    const { type, data } = action;

    switch (type) {
        default:
            return state;
    }
};

export default list;
